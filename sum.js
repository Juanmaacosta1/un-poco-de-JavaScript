// Creo una función básica que suma 2 números 
// Le paso los parámetros 
function suma(num1, num2) {
    const resultado = num1 + num2;

    // Devuelvo el resultado de la suma
    return resultado;
}

let resultado = suma(3, 5);
console.log(resultado); // Imprime el resultado
